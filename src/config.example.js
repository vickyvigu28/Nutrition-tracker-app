// Copy this file to config.js (already done for you in this project) and
// fill in real values there. config.js is gitignored so secrets don't get
// committed if this project is later pushed to a repo.
export const CONFIG = {
  OPENAI_API_KEY: '',
  GITHUB_TOKEN: 'ghp_xxxxx',
  GITHUB_OWNER: 'YOUR_ORG',
  GITHUB_REPO: 'family-nutrition',
  GITHUB_BRANCH: 'main'
};
